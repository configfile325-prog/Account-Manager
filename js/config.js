const CONFIG = {
    API_BASE: 'https://discord.com/api/v10',
    DELAYS: {
        FAST: 100,
        NORMAL: 300,
        SLOW: 500,
        VERY_SLOW: 1000
    }
};

let userToken = '';
let userData = null;
